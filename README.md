# WebM to MP4 转换器

一个使用Node.js和FFmpeg的WebM到MP4视频转换工具。

## 功能特点

- 🎬 支持多种视频格式转换 (WebM, MKV, AVI, MOV, FLV → MP4)
- 📁 批量转换功能
- 📊 实时转换进度显示
- ✅ 自动创建输入/输出目录
- 🔧 优化的编码设置
- 📋 详细的转换结果报告

## 系统要求

- Node.js (版本 14 或更高)
- FFmpeg (必须安装)

### 安装FFmpeg

**macOS (使用Homebrew):**
```bash
brew install ffmpeg
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
1. 下载FFmpeg: https://ffmpeg.org/download.html
2. 解压到任意目录
3. 将FFmpeg的bin目录添加到系统PATH

## 安装和使用

### 1. 安装依赖
```bash
npm install
```

### 2. 准备文件
将需要转换的WebM文件放入 `input` 目录（脚本会自动创建）

### 3. 运行转换
```bash
npm start
# 或者
node converter.js
```

### 4. 获取结果
转换后的MP4文件将保存在 `output` 目录中

## 项目结构

```
Youtube-DL/
├── input/          # 输入文件目录
├── output/         # 输出文件目录
├── converter.js    # 主转换脚本
├── package.json    # 项目配置
└── README.md       # 说明文档
```

## 编码设置

脚本使用以下优化的编码设置：

- **视频编码器**: H.264 (libx264)
- **音频编码器**: AAC
- **编码预设**: medium (平衡质量和速度)
- **质量设置**: CRF 23 (高质量)
- **优化**: faststart标志 (优化网络播放)

## 支持的文件格式

### 输入格式
- `.webm` - WebM视频文件
- `.mkv` - Matroska视频文件
- `.avi` - AVI视频文件
- `.mov` - QuickTime视频文件
- `.flv` - Flash视频文件

### 输出格式
- `.mp4` - MP4视频文件

## 使用示例

```javascript
const WebMToMP4Converter = require('./converter');

const converter = new WebMToMP4Converter();
converter.run();
```

## 故障排除

### FFmpeg未找到
确保FFmpeg已正确安装并添加到系统PATH中。

### 转换失败
- 检查输入文件是否损坏
- 确保有足够的磁盘空间
- 检查文件权限

### 性能优化
- 对于大文件，转换可能需要较长时间
- 可以调整编码预设来平衡质量和速度

## 许可证

MIT License
# youtube-dl
